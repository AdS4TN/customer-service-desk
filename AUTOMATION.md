# 业务自动化

入口：`/dashboard/automation`。在现有 AgentDesk 管理后台中配置规则、测试条件、查看执行记录，并进入关联会话、销售线索或工单。

## 使用流程

1. 选择模板并创建规则，填写名称、优先级、触发事件、条件与动作。优先级数字越小越先执行，同值按规则 ID 排序。
2. 使用样本测试条件。测试只返回整体与逐项匹配结果，不执行动作，也不能证明真实业务执行一定成功。
3. 保存草稿后单独启用。新规则默认停用；每次编辑保存都会停用并增加版本号，需要重新启用。
4. 在执行记录中检查状态、规则版本、动作快照、结果和错误；有权限时可重试失败记录。

## 支持范围

| 触发事件 | 条件 | 可执行动作 |
| --- | --- | --- |
| 收到客户消息 `message_received` | 渠道、文本包含 | 提取线索 `extract_lead`、创建工单 `create_ticket` |
| 创建销售线索 `lead_created` | 渠道、来源消息文本包含、自动标签 `lead_tag` | 添加自定义标签 `tag_lead`、分配负责人 `assign_lead` |

条件可选全部或任意匹配；没有条件时全部匹配。文本包含忽略大小写。渠道支持 Web、WhatsApp、Messenger。线索标签条件读取提取结果中的自动标签，添加标签动作写入自定义标签，两者用途不同。

提取线索动作复用会话记忆分析队列，结果 `extraction_queued` 表示已排队、正在处理或对应消息已处理，不保证产生实际销售线索。它依赖会话关联 AI 助手和现有提取服务。分配动作只处理没有负责人的线索，保留已有负责人；添加相同标签不会重复写入。创建工单要求每会话仅一次，生成关联来源消息的待处理内部工单，可指定负责人或保持未分配。

## 执行与恢复

- 每次从停用变为启用时，以该触发类型当前最大记录 ID 设置扫描水位，跳过启用前和停用期间的数据，不回放旧事件。
- 服务每 3 秒扫描一次，每个触发类型每批最多读取 50 个事件；实际完成时间还取决于积压和业务处理耗时。对每个事件按优先级处理所有适用规则，匹配后按定义顺序执行动作。
- 消息触发排除历史导入、非客户消息和已撤回消息。线索触发跳过已撤销或关闭的线索，校验来源消息及会话归属，排除来源全部为历史消息的线索。
- 按规则与事件保存去重收据；开启每会话仅一次时，使用会话作为去重范围。已有收据会阻止自动重复执行，编辑规则不会清空去重记录。
- 同一规则一次执行的业务写入在同一事务保存点内完成，任何动作失败都会回滚该次全部动作，同时保存失败记录并推进扫描。未匹配或不再有效的普通事件仅推进水位，不生成执行记录。
- 手动重试仅适用于仍启用、版本与失败记录一致的规则，重新校验当前来源、匹配条件、操作人权限和负责人状态。停用、重新启用或编辑会改变版本，旧失败记录不能直接重试。来源失效或条件不再匹配时，重试记录转为跳过。
- 保存和执行都校验权限；操作人被禁用或权限被撤销后，已启用规则也会执行失败。创建工单还要求工单创建权限。通知只在事务提交成功后推送给内部用户。

## 实现与参考

业务实现位于 `internal/services/automation_service.go`，扫描和收据访问位于 `internal/repositories/automation_repository.go`，轮询配置位于 `internal/services/cronx/cron.go`。它直接复用会话记忆、销售线索、通知、工单与工单编号的现有服务和仓储；工单入口位于 `internal/services/ticket_automation.go`。没有引入另一套业务数据模型来替代这些资源。

交互参考 Mixdesk 公开帮助文档：[如何配置自动化工作流](https://mixdesk.com/help/docs/automated-configuration/) 与 [设置不同的自动化分支流程](https://mixdesk.com/help/docs/automated-process-node/)。公开文档说明了规则、条件、动作和条件分支概念。此次无法访问需要认证的 Mixdesk 后台，不能把公开说明当作已核对后台布局与交互的证据。

当前交付是规则表单与执行记录，不是完整复刻旧工作流画布；暂不包含延迟事件、条件分支画布或向客户自动发送消息。

页面源码位于 `web/app/(dashboard)/dashboard/automation/page.tsx`，编辑器位于其 `_components/automation-editor.tsx`。列表复用与销售线索页面相同的 `DashboardListPage`；弹窗沿用现有 `ProjectDialog`；表单沿用 `Field`、`OptionCombobox` 及共享按钮、输入框、开关、标签页和 Lucide 图标。页面继续使用既有语义颜色、字号与组件变体。本次保留既有视觉体系，不新增 `DESIGN.md` 或全局设计令牌。

上述视觉一致性结论仅基于源码与现有组件复用证据。此次未完成浏览器桌面/移动端截图及双语交互检查，不代表已通过视觉验收。
